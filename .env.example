# Copy to .env for local development.
# The DB host is a project-specific loopback (see README) so ports never
# clash with other projects bound to 127.0.0.1.

HOST=0.0.0.0
PORT=3000

DB_HOST=billing-service.local
DB_PORT=5432
DB_USER=billing
DB_PASSWORD=billing
DB_NAME=billing
