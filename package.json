{
  "name": "recurring-payments",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Minimal recurring payment processing service skeleton",
  "license": "MIT",
  "main": "src/main.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "lint": "eslint . && prettier --check --no-error-on-unmatched-pattern \"**/*.js\" \"**/*.mjs\" \"**/*.json\" \"**/*.md\" \"**/*.ts\"",
    "fix": "eslint . --fix && prettier --write --no-error-on-unmatched-pattern \"**/*.js\" \"**/*.mjs\" \"**/*.json\" \"**/*.md\" \"**/*.ts\"",
    "test": "node --test test/"
  },
  "devDependencies": {
    "eslint": "^10.4.1",
    "eslint-config-metarhia": "^9.1.8",
    "prettier": "^3.8.1"
  }
}
